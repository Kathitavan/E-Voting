# ══════════════════════════════════════════════════════════════════
#  Secure E-Voting System — Backend Dependencies
#  Platform: Render.com (Python 3.10 — NOT 3.14)
#  Install:  pip install -r requirements.txt
#
#  ⚠️  IMPORTANT: Set Python version to 3.10.x in Render dashboard
#     Settings → Environment → Python Version → 3.10.13
#     face-recognition / dlib do NOT support Python 3.14
# ══════════════════════════════════════════════════════════════════

# ── Web Framework ──────────────────────────────────────────────────
Flask==3.0.3
Flask-Cors==4.0.1
gunicorn==22.0.0
Werkzeug==3.0.3

# ── Numerical ──────────────────────────────────────────────────────
numpy==1.26.4

# ── Computer Vision (headless = no GUI, correct for servers) ───────
opencv-python-headless==4.10.0.84

# ── dlib (C++ backbone — must install BEFORE face-recognition) ─────
# Render provides cmake via the render.yaml build command below
dlib==19.24.6

# ── Face Recognition ───────────────────────────────────────────────
face-recognition==1.3.0
face-recognition-models==0.3.0

# ── MediaPipe ──────────────────────────────────────────────────────
mediapipe==0.10.14

# ── Pinned transitive deps (prevent resolver version conflicts) ────
click==8.1.7
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5
blinker==1.8.2