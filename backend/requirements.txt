# ══════════════════════════════════════════════════════════════════
#  Secure E-Voting System — Backend Dependencies
#  Python: 3.11.9  |  Platform: Render.com
#
#  NO dlib / NO cmake — fully pre-built wheels only.
#  face-recognition replaced with insightface (binary wheel, no C++ build)
# ══════════════════════════════════════════════════════════════════

# ── Web Framework ──────────────────────────────────────────────────
Flask==3.0.3
Flask-Cors==4.0.1
gunicorn==22.0.0
Werkzeug==3.0.3

# ── Numerical ──────────────────────────────────────────────────────
numpy==1.26.4

# ── Computer Vision ────────────────────────────────────────────────
opencv-python-headless==4.10.0.84

# ── Face Recognition (replaces dlib + face-recognition) ───────────
# insightface has pre-built wheels — zero C++ compilation needed
insightface==0.7.3
onnxruntime==1.19.2

# ── MediaPipe ──────────────────────────────────────────────────────
mediapipe==0.10.32

# ── Pinned transitive deps ─────────────────────────────────────────
click==8.1.7
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5
blinker==1.8.2